var path = require('path');

var config = {
    development: {
        app: {
            name: "Open Med"
        }
    },
    test: {
        app: {
            name: "Open Med"
        }
    },
    production: {
        app: {
            name: "Open Med"
        }
    }
};

module.exports = config;